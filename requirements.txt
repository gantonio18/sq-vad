# SQ-VAD dependencies, pinned to the versions the reported results were produced with.
# Python 3.8.
#
#   pip install -r requirements.txt
#
# Offline reproduction (every figure and number in the paper) needs only the
# analysis block. torch is required to re-run the pose stream, anthropic to
# re-query the VLM, and ultralytics for the optional detector ablation.

# --- analysis: enough to regenerate every result from results/ and cache/ ---
numpy==1.23.5
scipy==1.10.1
pandas==2.0.3
pyarrow==17.0.0
scikit-learn==1.3.2
matplotlib==3.7.5
seaborn==0.13.2
opencv-python==4.13.0.92
tqdm==4.67.1
PyYAML==6.0.3

# --- re-running the frozen pose stream (STG-NF) ---
torch==1.12.1
torchvision==0.13.1

# --- re-querying the frozen VLM (needs ANTHROPIC_API_KEY; costs money) ---
anthropic==0.72.0

# --- optional: frozen COCO detector ablation (AGPL-3.0) ---
ultralytics==8.4.14
